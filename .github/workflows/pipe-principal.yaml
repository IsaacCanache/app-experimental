name: Main Workflow

on:
  push:
    branches: main
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Job principal ejecutándose..."

  trigger-logs:
    needs: build
    uses:  IsaacCanache/pipelines/.github/workflows/logs.yaml@619b152025a9069685e258159a3c49f27f969327
    with:
        run_id: ${{ github.run_id }}
        repositorio: ${{ github.repository }}




